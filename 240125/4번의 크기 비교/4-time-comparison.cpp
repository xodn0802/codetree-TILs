#include <iostream>
using namespace std;

int main() {
    int a, b, c, d, e;

    cin >> a >> b >> c >> d >> e;

    cout << (a > b) << endl;
    cout << (a > c) << endl;
    cout << (a > d) << endl;
    cout << (a > e) << endl;
    // 여기에 코드를 작성해주세요.
    return 0;
}